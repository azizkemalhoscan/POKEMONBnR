<%= simple_form_for([current_user, @user_pokemon]) do |f| %>
  <%= f.input :nickname, :label => "Give it a Nickname?"%>
  <%= f.association :pokemon, as: :select %>
  <%= f.button :submit %>
<% end %>